#ifndef function_h
#define function_h

#include <stdio.h>
float mnoz(float a, float b, float c);
float module(float umnozResult);
float kub(float c);
float division(float moduleResult, float kubResult);
float eks(float a);
float mnoz1(float divisionResult, float eksResult);

 #endif
 
